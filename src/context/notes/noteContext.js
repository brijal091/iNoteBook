import { createContext } from "react";


// What is Context 
    // Context will hold all the states related to out note

// Syntext to Creating new Context 
const noteContext = createContext();


export default noteContext;